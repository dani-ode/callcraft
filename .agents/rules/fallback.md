---
trigger: always_on
---

Forbidden to use hardcoding, including as a fallback mechanism. If an error occurs, surface the error explicitly instead of hardcoding a fallback value, as hardcoded fallbacks can cause confusion and maintenance issues in the future.