/**
 * Callcraft API Client Module (Barrel Exporter)
 * 
 * Re-exports all domain-driven API client modules from `@/lib/api` to maintain
 * 100% backward compatibility with existing imports across the application.
 */

export * from "./api";
